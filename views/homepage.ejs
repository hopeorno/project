<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AlUla - Home</title>

  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family:Arial, sans-serif; }

    body, html { height:100%; }

   .hero {
  position: relative;
  width: 100%;         /* كامل عرض الجهاز */
  height: 100vh;       /* كامل ارتفاع الشاشة */
  background: url('https://blog.umrahme.com/wp-content/uploads/2023/09/attraction_internal1-2.jpg')
   center center / cover no-repeat;
  display: flex;
  justify-content: center;
  align-items: center;
}


    /* dark overlay */
    .hero::before {
      content:"";
      position:absolute;
      top:0; left:0;
      width:100%; height:100%;
      background:rgba(0,0,0,0.45);
    }

    /* big title */
    .hero h3 {
      position:relative;
      font-size:18vw;
      color:white;
      font-weight:900;
      letter-spacing:5px;
      text-transform:uppercase;
      text-align:center;
      line-height:0.9;
    }

    /* top nav */
    nav {
      position:absolute;
      top:25px;
      right:40px;
    }

    nav a {
      color:white;
      margin-left:25px;
      text-decoration:none;
      font-size:16px;
      font-weight:bold;
    }

    /* button */
    .hero .btn {
      position:absolute;
      bottom:120px;
      padding:12px 35px;
      border:2px solid white;
      color:white;
      background:transparent;
      border-radius:30px;
      font-size:18px;
      cursor:pointer;
      transition:0.3s;
    }

    .hero .btn:hover {
      background:white;
      color:#000;
    }
  </style>

</head>
<body>

<div class="hero">

  <nav>
    <a href="/">Home</a>
    <a href="/about">About Us</a>
    <a href="/booking">Booking</a>
     <a href="/login">Login</a>
    <a href="/Contact">Contact</a>
  </nav>

  <h3>ALULA</h3>

  <button class="btn">WATCH TOUR</button>

</div>

</body>
</html>


